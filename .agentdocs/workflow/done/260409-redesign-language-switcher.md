# 语言切换器重构

## 背景
- 当前语言切换器仍采用下拉菜单结构，但站点实际只有 `zh`、`en`、`es` 三种语言。
- 用户希望彻底取消下拉交互，改为更有设计感的常驻切换按钮。
- 需要同时简化共享头部结构、样式与脚本逻辑，并保证桌面端和移动端都易于点击。

## 目标
- 将语言切换改为三语言常驻分段按钮。
- 移除下拉菜单、展开/收起和当前语言标签等冗余结构。
- 保持当前语言高亮、持久化和浏览器语言自动识别能力。

## TODO
- [x] 重构共享头部语言切换结构
- [x] 重写语言切换样式
- [x] 简化多语言脚本逻辑
- [x] 完成本地校验
